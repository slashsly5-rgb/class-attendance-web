---
phase: 01-project-setup-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - tailwind.config.ts
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - components.json
  - src/lib/utils.ts
autonomous: true
requirements:
  - NFR-7

must_haves:
  truths:
    - "Next.js development server runs successfully on localhost"
    - "TypeScript compilation works without errors"
    - "Tailwind CSS styling applies to components"
    - "shadcn/ui components can be installed via CLI"
  artifacts:
    - path: "package.json"
      provides: "Next.js 15 dependencies with TypeScript and Tailwind"
      contains: "next"
    - path: "tsconfig.json"
      provides: "TypeScript strict mode configuration"
      contains: "strict"
    - path: "components.json"
      provides: "shadcn/ui CLI configuration"
      contains: "style"
    - path: "src/app/page.tsx"
      provides: "Root page component"
      min_lines: 5
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "CSS import"
      pattern: "import.*globals\\.css"
    - from: "components.json"
      to: "src/components/ui"
      via: "component installation path"
      pattern: "components.*ui"
---

<objective>
Initialize Next.js 15 project with TypeScript, Tailwind CSS v4, and shadcn/ui component library.

Purpose: Establish the core frontend framework with modern tooling, type safety, and a beautiful component system for rapid UI development.

Output: Fully configured Next.js project running locally with development server, ready for feature development.
</objective>

<execution_context>
@C:/Users/slash/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/slash/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-setup-infrastructure/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Initialize Next.js 15 project with recommended configuration</name>
  <files>
    package.json
    tsconfig.json
    next.config.ts
    tailwind.config.ts
    src/app/layout.tsx
    src/app/page.tsx
    src/app/globals.css
    .gitignore
    .eslintrc.json
  </files>
  <action>
Run `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"` in the project root directory.

Accept all defaults during interactive prompts:
- TypeScript: Yes
- ESLint: Yes
- Tailwind CSS: Yes
- Use `src/` directory: Yes
- App Router: Yes
- Customize default import alias: No (use @/*)

After initialization:
1. Verify package.json contains Next.js 15.x
2. Enable TypeScript strict mode in tsconfig.json: set `"strict": true` in compilerOptions
3. Verify next.config.ts uses TypeScript (not .js)
4. Verify tailwind.config.ts exists (Tailwind v4 default)

Do NOT modify the default app structure yet - this task establishes the foundation only.
  </action>
  <verify>
    <automated>npm run dev</automated>
    <manual>Development server starts without errors, accessible at http://localhost:3000, displays Next.js default page</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>
Next.js 15 project initialized with TypeScript strict mode, Tailwind CSS v4, ESLint configured, development server runs successfully on localhost:3000, displays default Next.js landing page.
  </done>
</task>

<task type="auto">
  <name>Initialize shadcn/ui component library</name>
  <files>
    components.json
    src/lib/utils.ts
    src/components/ui/button.tsx
    tailwind.config.ts
  </files>
  <action>
Run `npx shadcn@latest init` to initialize shadcn/ui.

Interactive prompts - select these options:
- Style: Default
- Base color: Slate
- CSS variables: Yes
- Install location: src/components/ui
- Tailwind config: tailwind.config.ts
- Import alias for components: @/components
- Import alias for utils: @/lib/utils
- React Server Components: Yes

After initialization, install the button component as a test:
Run `npx shadcn@latest add button`

Verify installation:
1. components.json exists with correct paths
2. src/lib/utils.ts contains cn() helper function
3. src/components/ui/button.tsx exists
4. tailwind.config.ts updated with shadcn theme configuration

Do NOT modify component code - verify installation mechanics only.
  </action>
  <verify>
    <automated>cat components.json && cat src/lib/utils.ts && cat src/components/ui/button.tsx</automated>
    <manual>All three files exist with expected content, components.json has correct paths, button.tsx contains Button component with variants</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>
shadcn/ui initialized with components.json configuration, utils.ts helper created, button component installed successfully in src/components/ui/, Tailwind config extended with shadcn theme, CLI can install additional components.
  </done>
</task>

<task type="auto">
  <name>Create .env.example template file</name>
  <files>
    .env.example
  </files>
  <action>
Create .env.example file in project root with placeholder environment variable structure:

```
# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Optional: Service role key for server-side admin operations (keep secret!)
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
```

Add comments explaining:
- NEXT_PUBLIC_ prefix required for client-side access
- Actual values will be added in .env.local (gitignored)
- Service role key should NEVER have NEXT_PUBLIC_ prefix

Verify .gitignore already contains .env*.local (created by create-next-app).

Do NOT create .env.local yet - that happens when Supabase project exists.
  </action>
  <verify>
    <automated>cat .env.example && grep -q ".env*.local" .gitignore && echo "gitignore OK"</automated>
    <manual>.env.example exists with correct structure, .gitignore contains .env*.local pattern</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>
.env.example template created with Supabase environment variable placeholders and clear documentation, .gitignore verified to exclude .env.local files from version control.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Run `npm run dev` - server starts without errors
2. Visit http://localhost:3000 - Next.js default page loads
3. Check `npm run build` - TypeScript compilation succeeds
4. Verify `npx shadcn@latest add card` - can install additional components
5. Confirm .env.example exists and .env.local is gitignored
</verification>

<success_criteria>
- Next.js 15 development server runs successfully
- TypeScript strict mode enabled and compiling without errors
- Tailwind CSS v4 configured and applying styles
- shadcn/ui installed with button component available
- Project structure follows src/ directory pattern
- Environment variable template created
- Ready for Supabase integration and feature development
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup-infrastructure/01-01-SUMMARY.md`
</output>
